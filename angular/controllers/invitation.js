/* Controllers */
angular.module('wembliApp.controllers.invitation', []).

controller('InvitationCtrl', ['$scope', 'wembliRpc', '$window', 'plan',
	function($scope, wembliRpc, $window, plan) {
	}
]);
