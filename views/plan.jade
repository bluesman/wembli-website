extends layout
block frame2
	if (typeof plan.event.eventId === "undefined")
		include includes/no-event
	else
		//- if this plan has an organizer, then
		if ( (visitor.context === 'organizer') || ((visitor.context === 'visitor') && (!plan.organizer)))
			include includes/plan/organizer-view
		else
			include includes/plan/friend-view


