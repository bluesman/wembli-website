#parking-frame(ng-controller="ParkingCtrl")
	#map-background
		#map-container.google-map(lat="#{lat}",lng="#{lon}",markers="markers",map="map",center="center",zoom="zoom",draggable="true",mark-click="false",fit="true")
	#parking.pull-right
		.event-data(ng-cloak, event-id="{{eventId}}",direction="1")
		.event-social-links
		- console.log('plan prefs in interactive venuemap');
		- console.log(req.session.plan.preferences);
		- if (req.session.plan.preferences.payment === 'split-first')
			include parking/read-only
		- else
			//- include parking/buy-now

