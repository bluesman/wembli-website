#parking-frame
	#map-background
		#map-container.leaflet-map(zoom="14",lat="#{lat}",lon="#{lon}")
	#parking.pull-right
		.event-data(ng-cloak, event-id="{{eventId}}",direction="1")
		.event-social-links
		- console.log('plan prefs in interactive venuemap');
		- console.log(req.session.plan.preferences);
		- if (req.session.plan.preferences.payment === 'split-first')
			//- include interactive-venue-map/read-only
		- else
			//- include interactive-venue-map/buy-now

