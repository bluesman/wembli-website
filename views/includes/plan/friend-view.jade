//- should not ever load this partial if there is no plan in the session
#invitation-frame.animate-in(ng-controller="PlanCtrl")
	.event-data(event-id="#{req.session.plan.event.eventId}")
	#left this is the friend view foo
