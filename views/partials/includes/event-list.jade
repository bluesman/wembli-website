#event-list-wrapper
	- if (typeof heading !== "undefined")
		h3= heading
	ul
		li.event-wrapper(id="{{event.ID}}-wrapper",ng-repeat="event in events",ng-mouseleave="hideTicketSummary($event)",ng-mouseover="showTicketSummary($event)",ng-cloak)
			include event
