#ticket-filter-container
	h3 Filter:
	.qty-filter
		strong Qty:
		select#quantity-filter
			option --
			option(value="1") 1
			option(value="2") 2
			option(value="3") 3
			option(value="4") 4
			option(value="5") 5+
	.price-filter
		p
			label(for="amount") Price range:
			input#amount(type="text")
		#price-slider
#self-ticket-list-header-container.ticket-list-header-container
	a#price-header(href="#",ng-click="sortByPrice()") Price
	a#section-header(href="#",ng-click="sortBySection()") Section
	a#qty-header(href="#",ng-click="sortByQty()") Qty
ul#self-ticket-list-container.ticket-list-container
	li.ticket-row(ng-repeat="ticket in tickets")
		.ticket-section.hide {{ticket.Section}}
		.seat-row.hide {{ticket.Row}}
		.ticket-price
			.actual-price ${{ticket.ActualPrice}}
			.each (each)
		.ticket-info
			.ticket-section-name Section: {{ticket.Section}}
			.seat-row-name Row: {{ticket.Row}}
		.qty-container
			strong Qty:
			select(id="qty-{{ticket.ID}}",ng-model="ticket.selectedQty",name="qty-{{ticket.ID}}",ng-options="q for q in ticket.ValidSplits.int")
			.ticket-quantity.hide(ng-repeat="split in ticket.ValidSplits.int") {{split}}
		a.btn.btn-primary.btn-large.tickets-login-modal(ticket="{{ticket}}",ng-href="#tickets-login-modal-{{ticket.ID}}",ng-hide="customer") Buy
		a.btn.btn-primary.btn-large.buy-tickets-offsite(ticket="{{ticket}}",ng-href="{{tnUrl}}&tgid={{ticket.ID}}&evtid={{ticket.EventID}}&price={{ticket.ActualPrice}}&treq={{ticket.selectedQty}}&req.sessionId={{ticket.sessionId}}",ng-show="customer",target="_blank") Buy
