- if((typeof errors != "undefined") && (errors.passwordMismatch)) 
	.alert Passwords do not match.
#resetWrapper.well
	h3 Reset Your Password:
	form#loginForm.form-horizontal.well(method="post",action="/reset-password",enctype="application/x-www-form-urlencoded")
		input(type="hidden",name="token",value=token)
		input(type="hidden",name="email",value=email)
		fieldset
			.control-group
				label.control-label(for="password") Password:
				.controls
					input(type="password",value="",name="password")
			.control-group
				label.control-label(for="password2") Re-enter Password:
				.controls
					input(type="password",value="",name="password2")
			.form-actions
				button#loginSubmitButton.btn.btn-primary.btn-large Reset Password
